import 'package:flutter/material.dart';

class AppColorsLight {
  const AppColorsLight._();

  /// #7F3DFF
  static const primaryColor = Color(0xFF7F3DFF);

  static const Color appBarColor = Colors.teal;
  static const Color floatingActionButtonColor = Colors.cyanAccent;
  static const Color textButtonColor = Colors.deepOrange;
}
